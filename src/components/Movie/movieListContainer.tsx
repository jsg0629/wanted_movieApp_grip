import React from 'react'
import style from './MovieListContainer.module.scss'

const MovieListContainer = (): JSX.Element => {
  return (
    <div className={style.movieListContainer}>
      MovieListContainer
    </div>
  )
}

export default MovieListContainer
